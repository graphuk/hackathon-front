{{wish-santa}}
{{#if isFilled}}
	{{wish-message prefix=prefix message=message}}
{{else}}
	{{wish-form actionToCall="add"}}
{{/if}}
<div class="story-link">
	{{#link-to "story"}}Истории пожеланий{{/link-to}}
</div>
